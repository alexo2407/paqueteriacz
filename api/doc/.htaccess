# Documentación API - Servir archivos estáticos directamente
# Compatible con Apache 2.2 y 2.4

Options -Indexes

# Servir index.php como DirectoryIndex
DirectoryIndex index.php index.html

# No aplicar rewrites del directorio padre
<IfModule mod_rewrite.c>
    RewriteEngine On
    # Detener cualquier rewrite heredado
    RewriteRule .* - [L]
</IfModule>

